# `nypst`

Inline [Typst](https://typst.app) code directly into Nushell, and translate Nu datatypes directly into Typst equivalents.

See the [example](./example.nu), which you can run with `nu example.nu` from this folder.
